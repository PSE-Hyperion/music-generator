x-default-service: &default-service
  build:
    context: ../
    dockerfile: .docker/Dockerfile
  image: groove-panda:default
  volumes:
      - ../data/:/app/data:cached
  working_dir: /app
  user: "1000:1000"
  stdin_open: true
  tty: true

services:
  app:
    <<: *default-service

